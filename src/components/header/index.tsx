import { Container, Wrapper, Logo, Button } from "./styles";

export const Header = () => {
  return (
    <Container>
      <Wrapper>
        <Logo>Marcus Davanco</Logo>
        <Button>Contact</Button>
      </Wrapper>
    </Container>
  );
};
